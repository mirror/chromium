module perfetto.mojom;

struct DataSourceConfig {
  string name;
  uint32 target_buffer;
  string trace_category_filters;
};

interface Producer {
    OnConnect();
    SetTraceBuffer(handle<shared_buffer> shared_memory, uint64 size);
    OnDisconnect();
    CreateDataSourceInstance(int64 id, DataSourceConfig config);
};

