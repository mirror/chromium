module perfetto.mojom;

import "producer.mojom";

struct DataSourceConfig {
  string name;
  uint32 target_buffer;
  string trace_category_filters;
};

interface ProducerClient {
    ConnectProducer(Producer producer);
    EnableDataSource(handle<shared_buffer> shared_memory, uint64 size);
    DisableDataSource();
};
