module perfetto.mojom;

import "producer.mojom";

struct DataSourceDescriptor {
    string name;
};

interface ProducerService {
  ConnectProducer(Producer producer);
  RegisterDataSource(DataSourceDescriptor descriptor);
  NotifySharedMemoryUpdate(array<uint32> changed_pages);
};

