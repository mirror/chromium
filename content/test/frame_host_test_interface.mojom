// Copyright 2017 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

module content.mojom;

import "url/mojo/url.mojom";

// Toy interface used in RenderFrame/RenderFrameHost tests to exercise issuing
// InterfaceRequests to the InterfaceProviders handed out via mojom.FrameHost.
//
// The `Annotate` method is invoked by clients immediately after making the
// InterfaceRequest, so as to annotate where the InterfaceRequest originates
// from. This allows verification that the request was delivered to the right
// InterfaceProvider and was bound to the right implementation.
interface SourceAnnotatedInterface {
  AnnotateSource(url.mojom.Url url, string event);
};
