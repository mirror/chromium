include_rules = [
  "+content/public/browser",
  "+crypto/random.h",
  "+device/geolocation",
  "+gpu/config/gpu_info.h",
]

specific_include_rules = {
  '.*_[a-z]*test\.cc': [
    "+content/public/test",
  ],
}
