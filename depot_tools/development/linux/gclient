#!/bin/sh

base_dir=$(dirname "$0")

# Update the "release" subdirectory every time this script is run to
# stay up-to-date with the latest depot_tools release.
"$base_dir/bootstrap/update.sh" "$base_dir/release"
exec "$base_dir/release/gclient.sh" "$@"
