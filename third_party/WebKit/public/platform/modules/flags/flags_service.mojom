// Copyright 2017 The Chromium Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

module blink.mojom;

import "url/mojo/origin.mojom";

enum FlagMode {
  SHARED,
  EXCLUSIVE
};

// TODO: Replace id/ReleaseFlag with a interface

interface FlagsService {
  const int64 kFlagTimeout = -1;

  RequestFlag(url.mojom.Origin origin, array<string> scope, FlagMode mode) => (int64 flag_id);
  ReleaseFlag(url.mojom.Origin origin, int64 flag_id);
};
