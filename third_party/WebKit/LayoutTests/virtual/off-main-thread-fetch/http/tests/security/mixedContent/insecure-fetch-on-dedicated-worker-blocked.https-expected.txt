This test starts a dedicated worker that loads insecure data via Fetch API. We should trigger a mixed content callback and block the load.

The test passes if the result is "LOAD FAILED".

Result: LOAD FAILED

